// Test file for pre-commit hook
// This file will be auto-formatted and fixed

const badlyFormatted = { message: 'Hello World', value: 123 };

function testFunction(param1: string, param2: number) {
  console.log(badlyFormatted);
  return param1 + param2;
}

export default testFunction;
